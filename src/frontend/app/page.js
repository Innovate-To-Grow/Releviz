import HomePageClient from "@/components/HomePageClient";

export const metadata = {
  title: "Releviz | Find a time that works for everyone",
  description:
    "Create a group scheduling poll, collect availability, and find the best meeting time.",
};

export default function Home() {
  return <HomePageClient />;
}
